
let firstName;
let lastName = 'rayo';

// console.log(`${firstName || 'No firstName'}  ${lastName || 'No lastName'}`)

const isActive = true;

const message = ( isActive === true )
                 ? ' Activo' 
                 : 'Inactivo'

console.log(message)